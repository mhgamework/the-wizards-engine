using System;
using System.Collections.Generic;
using System.Text;

namespace MHGameWork.TheWizards.ServerClient.Editor
{
    /*public class TerrainObject : Database.IGameObject //TODO: ': IGameObject' ??
    {

        private string relativeFilename;

        public string RelativeFilename
        {
            get { return relativeFilename; }
            //set { relativeFilename = value; }
        }

        public void SetRelativeFilename(string value)
        {
            relativeFilename = value;
        }
	

        public TerrainObject()
        {
            fullData = new EditorTerrainFullData();
        }






        private EditorTerrainFullData fullData;

        /// <summary>
        /// This should use TerrainFullData instead of EditorTerrainFullData
        /// </summary>
        /// <returns></returns>
        public EditorTerrainFullData GetFullData()
        {
            //This should dynamically load the data from disk (maybe using loading queue, asynchronous)
            // and cache the data
            return fullData;
        }


        /// <summary>
        /// Temporary, not sure where to place this
        /// </summary>
        /// <param name="data"></param>
        public void TempSetFullData( EditorTerrainFullData data )
        {
            fullData = data;
        }

        #region IDisposable Members

        public void Dispose()
        {
            fullData = null;

        }

        #endregion
    }*/
}
